name: Regenerate documentation

on:
  repository_dispatch:
    types: [docs-update]
  workflow_dispatch:
    inputs:
      ref:
        description: 'openclaw branch or ref to generate from'
        required: false
        default: 'main'

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout octo-docs
        uses: actions/checkout@v4

      - name: Checkout openclaw
        uses: actions/checkout@v4
        with:
          repository: JeffSteinbok/openclaw
          token: ${{ secrets.OPENCLAW_TOKEN }}
          path: openclaw
          ref: ${{ github.event.client_payload.ref || github.event.inputs.ref || 'main' }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install pyyaml

      - name: Generate docs
        run: python skills/generate-docs/generate-docs.py --source ./openclaw --output-dir ./docs

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet docs/ && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          git commit -m "docs: auto-regenerate from openclaw@${GITHUB_EVENT_CLIENT_PAYLOAD_REF:0:7}"
          git push
