---
layout: default
title: Plugins
nav_order: 4
---

# Plugins

*Generated by an AI skill reading the live OpenClaw configs from Octo.* 🐙

● List directory .
  └ 7 files found

● List directory openclaw
  └ 14 files found

● List directory openclaw/plugins
  └ 12 files found

● Glob "**/*.md" in /home/runner/work/octo-docs/octo-docs/docs
  └ 5 files found

● Read docs/skills.md
  └ 14 lines read

● Read docs/index.md
  └ 100 lines read

● Read docs/services.md
  └ 20 lines read

● Read docs/jobs.md
  └ 66 lines read

Now I have enough context. Let me generate the plugin documentation.

● List directory openclaw/plugins/outlook-calendar
  └ 6 files found

Here is the generated documentation section:

---

## 🔧 config-backup

Backup OpenClaw config to Git with automatic change detection.

**Capabilities:**
- Detects changes to openclaw.json and commits only when needed
- Designed for cron — runs silently unless something goes wrong
- Supports forced commits for manual snapshots

---

## 🔧 fastmail-send

Send emails and calendar meeting invitations via Fastmail JMAP.

**Capabilities:**
- Compose and send plain-text emails with optional CC and attachments
- Create meeting requests with accept/decline buttons (iCalendar)
- Configurable signature and sender identity

---

## 🔧 hass-camera-snapshot

Capture snapshots from home security cameras via Home Assistant.

**Capabilities:**
- Snap any individual camera or all cameras at once
- Automatically downloads and timestamps images locally
- Pre-flight checks with actionable error messages

---

## 🔧 homeassistant-cli

Advanced Home Assistant control using the official hass-cli tool.

**Capabilities:**
- Control lights, switches, climate, locks, and alarm systems
- Query entity states, history, and event logs
- Auto-completion and rich output formatting

---

## 🔧 ics-calendar

Fetches calendar events from a published ICS feed (Outlook Live).

**Capabilities:**
- Fetch upcoming events from a published ICS calendar feed
- Parses standard iCalendar format

---

## 🔧 opentable

Check real-time restaurant availability on OpenTable.

**Capabilities:**
- Look up restaurant IDs from OpenTable URL slugs
- Query available reservation slots by date, party size, and time
- Returns direct booking links for available times

**Dependencies:** `requests>=2.28.0`, `curl_cffi>=0.7.0`

---

## 🔧 opentable-heartbeat

Health-check for the OpenTable plugin. Alerts on failure via configured notification channel.

**Capabilities:**
- Run health checks against the OpenTable API
- Returns status (`ok` or `error`) with a descriptive message
- Alerts via notification channel on failure

---

## 🔧 personal-calendars

Fetch personal and family calendars from Outlook Live ICS feeds.

**Capabilities:**
- Query personal, family, or all calendars by date range
- Returns events with subject, time, and location
- Supports multiple calendar sources in a single query

---

## 🔧 outlook-mail

Search and read Outlook inbox via Microsoft Graph API.

**Capabilities:**
- List and browse recent inbox messages
- Search messages by query, sender, subject, or date range
- Read full message content by ID

---

## 🔧 outlook-work-calendar

Fetch work calendar events from a published Outlook endpoint.

**Capabilities:**
- Query events by date range with subject, time, and location
- Shows busy status and sensitivity level for scheduling
- No authentication required — uses published calendar feed

---

## 🔧 weightwatchers

Search foods, log meals, view diary and points budget via the unofficial WW API.

**Capabilities:**
- View daily food diary with tracked meals and points summary
- Search the WW food database for foods and portion options
- Log food items to the diary
- Calculate SmartPoints offline from nutrition data
- Check remaining points budget for any date

---
